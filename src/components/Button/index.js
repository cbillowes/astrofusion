import React from 'react';

const Button = () => {
  return <button>Click me!</button>;
};

export const PrimaryButton = () => {
  return <button>I am a Primary Button</button>;
};

export default Button;
